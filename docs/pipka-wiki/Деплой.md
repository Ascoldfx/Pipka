#deploy

# Деплой

## Окружение

| Параметр | Значение |
|----------|---------|
| Сервер | Contabo VPS, Ubuntu 24.04 |
| SSH | `ssh pipkaops@217.76.61.28 -i ~/.ssh/id_ed25519` + `sudo` |
| Каталог | `/opt/pipka` |
| Домен | `pipka.net` через Cloudflare proxy |
| Стек | Docker Compose: `pipka-app-1` + `pipka-db-1` |

## Контейнеры

`docker-compose.yml`:

- **app** — FastAPI + Telegram bot + APScheduler. UID/GID `10001`, `read_only`, `cap_drop: ALL`, `no-new-privileges`; writable только `app_data` и tmpfs. Порт `127.0.0.1:8000` (за nginx).
- **db** — `postgres:16-alpine`. Volume `pgdata` под `/var/lib/postgresql/data`. Healthcheck `pg_isready -U pipka` каждые 5с.

App ждёт healthy db (`depends_on.db.condition: service_healthy`). Логи каждого контейнера ротируются по `10MB × 3` файла (json-file driver).

## Стандартный цикл деплоя

```bash
# Локально
git add <files> && git commit -m "..." && git push origin main

# На сервере
ssh pipkaops@217.76.61.28 -i ~/.ssh/id_ed25519
cd /opt/pipka && sudo git pull && sudo docker compose up -d --build
```

`--build` обязателен — без него Docker подцепит закэшированный image и код не обновится.

## Что происходит на старте контейнера

1. `init_db()` (см. `app/database.py`) запускает `alembic upgrade head` через `run_in_executor`. Подробнее — [[Миграции]].
2. FastAPI поднимается на `0.0.0.0:8000`.
3. Telegram bot стартует long-polling к Telegram API.
4. APScheduler регистрирует jobs (см. [[Сервисы#scheduler]]) и запускает первый `startup_scan` через 30с.
5. Если задан `SENTRY_DSN` — инициализируется SDK ДО создания FastAPI app (см. [[Observability]]).

## Конфигурация

Все настройки в `/opt/pipka/.env`. Полный список — [[Настройки]]. Минимальный набор для запуска:

```env
TELEGRAM_BOT_TOKEN=...
ADZUNA_APP_ID=...
ADZUNA_APP_KEY=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
SESSION_SECRET=<32+ random bytes>
POSTGRES_PASSWORD=...
```

Опционально включаются: `GEMINI_API_KEY`, `NVIDIA_API_KEY`, `B2_KEY_ID/B2_APP_KEY/B2_BUCKET`, `SENTRY_DSN`. Каждый — лениво активируется при заполнении (см. [[Скоринг]], [[Бэкапы]], [[Observability]]).

## Откат

```bash
ssh pipkaops@217.76.61.28 -i ~/.ssh/id_ed25519
cd /opt/pipka
git log --oneline -10           # выбрать предыдущий зелёный коммит
git checkout <sha>
docker compose up -d --build
```

Миграции БД не откатываются автоматически: Alembic `downgrade` не вызывается. Если нужно — вручную `alembic downgrade -1` в контейнере. См. [[Миграции]].

## Production snapshot: 23.09.2026

- DB migration head: `0013_application_identity`.
- Container `pipka-app-1` был healthy; `/health` вернул database/scheduler `ok`.
- Runtime на VPS обновлён до локальных исправлений `34a7f81`, `db1c96c`, `2dee88f`. Проверены совпадающие SHA-256 пяти runtime-файлов.
- VPS checkout HEAD остаётся `4a29d79`: runtime изменения доставлены архивным overlay, а не `git pull`. Поэтому для следующего deploy сначала синхронизируйте/зафиксируйте repository checkout с runtime. Не перезапускайте старый HEAD как будто он содержит текущие исправления.
- Production `.env`: `/opt/pipka/.env`, `0600 root:root`. Секретные значения в wiki не хранить.
- Backups каждого overlay хранятся на VPS как `/tmp/pipka-pre-34a7f81-runtime.tgz`, `/tmp/pipka-pre-db1c96c-runtime.tgz`, `/tmp/pipka-pre-2dee88f-runtime.tgz` (root-owned). Срок хранения и удаление не проверялись.
- Действующая модель fallback NVIDIA и production access inventory: [[Текущее состояние и доступы]].

## Проверка после деплоя

```bash
ssh pipkaops@217.76.61.28 -i ~/.ssh/id_ed25519
docker logs pipka-app-1 --since 2m | grep -E 'Bot is running|Traceback|alembic'
curl https://pipka.net/health
docker exec pipka-db-1 psql -U pipka -d pipka -c \
  "SELECT created_at, event_type, status, left(message,80) FROM ops_events \
   WHERE created_at > now() - interval '15 minutes' ORDER BY created_at DESC LIMIT 5;"
```

Триада: бут → health 200 → свежий `scan success` в [[Ops панель|ops_events]].

`/health` проверяет `SELECT 1`, APScheduler и возраст последнего scan. `/health/live` — liveness. Docker healthcheck имеет `start-period=90s`.

Длинные data-миграции выполняются отдельно:

```bash
sudo docker compose stop app
sudo docker compose run --rm --no-deps app alembic upgrade head
sudo docker compose up -d app
```

SSH hardened: root/password login запрещены, key-login только для `pipkaops`; Fail2ban `sshd` активен. Origin HTTP/HTTPS разрешён UFW только из Cloudflare ranges.

→ [[Архитектура]] → [[Настройки]] → [[Миграции]] → [[Observability]]
