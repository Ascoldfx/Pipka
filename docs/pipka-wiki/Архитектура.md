#service

# Архитектура

## Общая схема

```
                        ┌─────────────────────────────────┐
                        │         pipka.net               │
                        │      (Cloudflare Proxy)         │
                        └──────────────┬──────────────────┘
                                       │ HTTPS
                        ┌──────────────▼──────────────────┐
                        │             Nginx               │
                        │         → :8000 (loopback)      │
                        └──────────────┬──────────────────┘
                                       │
          ┌────────────────────────────▼──────────────────────────┐
          │                pipka-app-1 (Docker)                   │
          │                                                       │
          │  ┌──────────────┐   ┌────────────┐   ┌─────────────┐  │
          │  │  FastAPI     │   │  Telegram  │   │ APScheduler │  │
          │  │  :8000       │   │  Bot       │   │             │  │
          │  │              │   │            │   │ • scan/3h   │  │
          │  │  /api/*      │   │  /start    │   │ • backfill  │  │
          │  │  /auth/*     │   │  /search   │   │   /2h       │  │
          │  │  /health     │   │  /apply    │   │ • cleanup   │  │
          │  └──────┬───────┘   └─────┬──────┘   │   /03:00    │  │
          │         │                 │           │ • backup    │  │
          │         │                 │           │   /02:30    │  │
          │         │                 │           │ • url-check │  │
          │         │                 │           │   /04:00    │  │
          │         │                 │           │ • nvidia    │  │
          │         │                 │           │   rescore   │  │
          │         │                 │           │   /30min    │  │
          │         │                 │           │ • watchlist │  │
          │         │                 │           │   /6h       │  │
          │         │                 │           │ • embed-idx │  │
          │         │                 │           │   /2h       │  │
          │         └─────────────────┴───────────┴──────┬──────┘  │
          │                          │                            │
          │              ┌────────────▼────────────┐               │
          │              │     SQLAlchemy async    │               │
          │              │     Alembic migrations  │               │
          │              └────────────┬────────────┘               │
          └───────────────────────────┼───────────────────────────┘
                                      │ asyncpg
          ┌───────────────────────────▼───────────────────────────┐
          │                  pipka-db-1 (Docker)                  │
          │                  PostgreSQL 16                        │
          │                  (jsonb, GIN, hot-path indexes)       │
          └───────────────────────────────────────────────────────┘
```

Подробнее о развёртывании — [[Деплой]].

## Поток данных: Сканирование вакансий

```
APScheduler (каждые 60 минут) → _background_scan()
     │
     ├─► JobAggregator.search() — параллельно, 9 источников
     │       Adzuna · JobSpy (LinkedIn/Indeed/Glassdoor) ·
     │       Arbeitnow · Remotive · Arbeitsagentur ·
     │       Xing · BerlinStartupJobs · WTTJ · Jooble
     │
     ├─► Дедупликация (exact title+company+country+location + fuzzy guards)
     │   См. [[Дедупликация]]
     │
     ├─► Bulk UPSERT в jobs (3 round-trip'а вместо N+1)
     │
     └─► Для каждого пользователя:
            ├─ pre_filter() — без AI (см. [[Скоринг#этап-1]])
            ├─ score_jobs_gemini/nvidia (см. [[Скоринг#распределение]])
            ├─ UPSERT в job_scores с profile_hash/model_version
            │   (см. [[Кэш и инвалидация]])
            └─ Push в Telegram если score ≥ 80 (max 10/прогон)
```

Параллельные поллеры:
- `_backfill_score` (каждые 2ч) — оценивает накопленные unscored.
- `_nvidia_idle_rescore` (каждые 30мин) — пере-оценка DE, когда основная очередь пуста.
- `_watchlist_scan` (каждые 6ч) — точечный скан компаний из `target_companies`.
- `_cleanup_old_jobs` (03:00 UTC) — удаление вакансий >45 дней.
- `_daily_backup` (02:30 UTC) — pg_dump → B2. См. [[Бэкапы]].
- `_check_job_urls` (04:00 UTC) — drain-loop HEAD-пинг закрытых ссылок. См. [[Проверка ссылок]].
- `_embed_index` (каждые 2ч + startup +90с) — fill `jobs.embedding` / `user_profiles.embedding`; NVIDIA burst каждые 30 минут добавляет jobs-пакет, когда scoped очередь >100. См. [[Поиск и индексация]].

## Поток данных: Авторизация

```
Browser → /auth/google/login
    → Google OAuth2 (authlib)
    → /auth/google/callback
    → get_or_create_google_user()
    → request.session["user_id"] (signed cookie pipka_session)
    → Redirect → /
```

Подробнее — [[Auth]]. CSRF на mutating-эндпоинтах — [[Безопасность#3. CSRF (double-submit)]].

## Файловая структура

Снимок на 23.09.2026.

```
/opt/pipka/
├── alembic/
│   ├── env.py
│   └── versions/                 # 0001 … 0013, см. [[Миграции]]
│       ├── 0001_baseline.py … 0009_geographic_dedup_hash.py
│       ├── 0010_nemotron_embeddings.py          # vector(768) → vector(2048)
│       ├── 0011_user_billing_and_transactions.py
│       ├── 0012_onboarding_and_feedback.py
│       └── 0013_application_action_identity.py  # production head
├── app/
│   ├── api/                      # 11 роутеров, см. [[API]]
│   │   ├── _helpers.py           # auth/role хелперы
│   │   ├── _ratelimit.py         # sliding-window лимитер
│   │   ├── auth.py               # Google OAuth + /api/me
│   │   ├── health.py             # /health, /health/live
│   │   ├── pages.py              # /, /llms.txt, /infographic
│   │   ├── jobs.py               # /api/countries, /api/jobs, /action, /analyze
│   │   ├── stats.py              # /api/stats + /api/public/stats
│   │   ├── profile.py            # /api/profile + /api/profile/resume
│   │   ├── scan.py               # /api/scan + /api/scan/status
│   │   ├── ops.py                # /api/ops/*
│   │   ├── admin.py              # /api/admin/user/{id}/*
│   │   ├── billing.py            # /api/billing/*, /api/webhooks/crypto — [[Биллинг и кредиты]]
│   │   └── feedback.py           # /api/feedback, /api/onboarding/complete
│   ├── bot/
│   │   ├── bot.py                # ApplicationBuilder + регистрация handlers
│   │   ├── formatters.py         # format_job_card
│   │   ├── keyboards.py          # inline-меню
│   │   └── handlers/             # start inbox search results tracker settings
│   ├── models/
│   │   ├── user.py               # User, PaymentTransaction, UserFeedback, UserProfile
│   │   ├── job.py                # Job, JobScore
│   │   ├── application.py        # Application, ApplicationHistory, SearchSubscription
│   │   └── ops_event.py          # OpsEvent
│   ├── scoring/
│   │   ├── rules.py              # pre_filter (без AI) — [[Pre-filter правила]]
│   │   ├── matcher.py            # промпт + маршрутизатор скоринга
│   │   ├── gemini_client.py      # общий async-клиент google-genai
│   │   ├── gemini_matcher.py     # Gemini 3.6 Flash + circuit breaker + дневной лимит
│   │   ├── nvidia_matcher.py     # NVIDIA Poolside Laguna streaming fallback
│   │   ├── nvidia_embedding_client.py  # клиент NVIDIA /v1/embeddings
│   │   └── profile_hash.py       # compute_profile_hash + MODEL_* фабрики
│   ├── services/
│   │   ├── scheduler_service.py  # APScheduler jobs: scan, score, backfill, embed …
│   │   ├── user_scope_service.py # справедливые мульти-юзер планы — [[Мульти-юзер очереди]]
│   │   ├── billing_service.py    # Cryptomus invoice/подпись/начисление
│   │   ├── tracker_service.py    # apply/reject/save + auto-exclude
│   │   ├── user_service.py       # OAuth get_or_create_google_user
│   │   ├── ops_service.py        # record_ops_event + build_ops_overview
│   │   ├── backup_service.py     # pg_dump + gzip + B2 + restore-check
│   │   ├── url_checker.py        # HEAD-ping classifier (active|closed|unreachable)
│   │   ├── embedding_service.py  # Nemotron embeddings + pgvector retrieval
│   │   ├── resume_parser.py      # изолированный парсинг резюме — [[Resume parsing]]
│   │   └── job_service.py        # вспомогательные
│   ├── sources/                  # 11 источников — [[Источники вакансий]]
│   │   ├── aggregator.py         # bulk upsert + фильтры + dedup
│   │   ├── base.py               # JobSource, RawJob, SearchParams, dedup_hash
│   │   ├── country_queries.py    # пары «страна → роли» для provider API
│   │   ├── adzuna.py jobspy_source.py arbeitnow.py remotive.py
│   │   ├── arbeitsagentur.py xing.py berlinstartupjobs.py wttj.py
│   │   ├── jooble.py builtin.py gupy.py
│   │   └── watchlist.py          # точечный скан target_companies
│   ├── static/
│   │   ├── dashboard.html        # SPA каркас
│   │   ├── infographic.html      # публичная SMM-инфографика
│   │   ├── llms.txt              # манифест для AI-краулеров
│   │   ├── css/styles.css
│   │   └── js/
│   │       ├── security.js       # fetch wrapper c CSRF
│   │       └── events.js         # CSP-safe delegated UI events
│   ├── config.py                 # pydantic-settings — [[Настройки]]
│   ├── database.py               # async engine + get_db
│   ├── security_headers.py       # CSP (nonce-only scripts), HSTS и др.
│   ├── security_probes.py        # распознавание ботовых vulnerability-проб для Ops
│   └── main.py                   # FastAPI app + middleware
├── tests/                        # 40 файлов, 158 тестов — [[Тесты]]
├── scripts/
│   ├── rescore_countries.py      # разовый пересчёт выбранных стран
│   ├── check_secrets.py          # CI: поиск credential-shaped строк
│   └── check_inline_js.mjs       # CI: запрет inline JS (CSP)
├── .github/workflows/ci.yml      # тесты + миграции + проверки скриптов
├── docker-compose.yml
├── Dockerfile
├── alembic.ini
├── pyproject.toml
├── run.py                        # точка входа
└── .env                          # на сервере, не в git (шаблон — .env.example)
```

→ [[Сервисы]] → [[База данных]] → [[API]] → [[Скоринг]] → [[Деплой]]
